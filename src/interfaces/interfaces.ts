export type ITypeIcon = 'work' | 'chat' | 'coffee' | 'gym' | 'book' | 'alarm' | ''

export enum TaskStatus {
  WONT_DO = 'wont_do',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  PENDING = 'pending',
}
